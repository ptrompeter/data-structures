language: python
python:
  “2.7”
  “3.5”
# command to install dependencies
#install: “pip install -r requirements.txt” (or which ever file you what to load from, like pip install -e .[dev,test])
install: "pip install -e .[test]"
# command to run tests
script: aloe